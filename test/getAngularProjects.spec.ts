import {expect} from "chai";
import {getAngularProjects} from "../src/getAngularProjects";

describe('My blerp test', () => {
    it('should test that', () => {
        getAngularProjects(__dirname);
    });
});
